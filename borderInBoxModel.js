/*
==================================================================
                            DEPRECATED
        please check the documentation and use plugin rules
==================================================================
*/

import config from './config/extendedStylelintOrderConfig';

const borderInBoxConfig = config({ 'border-in-box-model': true });

export default borderInBoxConfig;
